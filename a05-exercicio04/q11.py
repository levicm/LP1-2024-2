preco = float(input('Digite o preço do produto: '))
reajuste = preco * 0.05
preco = preco + reajuste
print('O preço do produto após o reajuste é: ', preco)
# outra possibilidade:
# preco = preco + (preco * 0.05)

# outra possibilidade:
# preco = preco * 1.05
